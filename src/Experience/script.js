import './style.css'
import Experience from './Experience.js'

const experience = new Experience(document.querySelector('canvas.webgl')) // we give the canvas to the Experience class so the canvas can be changed at the top of the project2 